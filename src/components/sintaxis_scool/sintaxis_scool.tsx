// SintaxisScool.tsx
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';

const SintaxisScool = () => {
  
  return (
    <div className="SintaxisScool">
     
    </div>
  );
};

export default SintaxisScool;