// SintaxisTree.tsx
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';

const SintaxisTree = () => {
  
  return (
    <div className="SintaxisTree">
     
    </div>
  );
};

export default SintaxisTree;